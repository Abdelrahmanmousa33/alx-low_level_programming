#include "main.h"

/**
 * print_diagonal - check the code
 * @n: input
 * Return: Always 0.
 */
void print_diagonal(int n)
{
	int a, b;

	for (a = 0; a < n; a++)
	{
		for (b = 0; b < a; b++)
		{
			_putchar(" ");
		}
		_putchar(\);
		_putchar('\n');
	}





}
