#include "main.h"
/**
 * print_numbers - chexk up
 * Return: 1(sucsses),0(otherwise)
 */
int print_numbers(void)
{
	for (int c = 48; c <= 57; c++)
	{
		_putchar(c);
	}
	_putchar('\n');
	return (0);
}
