#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strcat - check the code
 *@dest:input
 *@src:input
 * Return: Always 0.
 */
char *_strcat(char *dest, char *src)
{
	int n;

	char *ptr = &dest[0];

	n = strlen(dest);
	dest[n] = *src;
	return (ptr);


}
